# 莫尔斯码二叉树解码器 — 原理图查漏补缺与优化方案（对照 V1.3）

> 生成日期：2026-07-28 ｜ 方法：通过 easyeda-api 实时枚举两页全部元件/网络标识/导线，并用引脚坐标反查网络归属
> 状态结论：**原理图为半成品，存在若干连接硬伤；网络标签使用严重不足**（与"尽量使用网络标签"的要求相悖）

---

## 1. 现状概览

工程 `莫尔斯码二叉树`（Schematic1）分两页：

| 页面 | 主要内容 |
|---|---|
| **P1 页** | U1 CH32V003F4P6（MCU）+ USB-C(USBC1)、按键 S1/S2、电位器(RP1 区)、蜂鸣器 BUZZER1+驱动 Q1、SWIO 焊盘(H1)、扩展排针 J4、去耦 C1~C8 |
| **LED 页** | U2~U6 五片 74HC595 + D1~D37 共 37 颗 LED + R1~R37 限流电阻 |

元件放置基本齐全（U1~U6、D1~D37、R1~R37、电源/输入/蜂鸣器/连接器均在位）。

**网络标签使用统计**：P1 页对本地信号用了网络标签（`PA1_KEY`、`PA2_CLR`、`PC4_ADC`、`UART_TX`、`SWIO`、`SPARE_*`、`PD0_TONE`、`595 DATA（SER）` 等）；**LED 页网络标签数 = 0**，完全依赖一根巨型无名飞线（`net` 全为 `"(none)"`）把 595 输出/电阻/LED 连成同一网络。

---

## 2. 问题清单（按严重程度）

### 🔴 严重（不修无法工作 / 有烧毁风险）

**P1-1　MCU 控制脚错标（三脚同名为 `PC4_ADC`）**
- U1 引脚：`PC1`(应 SRCLK)、`PC2`(应 RCLK)、`PC3`(应 OE) 三脚网络名**全部被错标为 `PC4_ADC`**（典型复制粘贴错误）。
- 后果：595 的时钟(SRCLK)/锁存(RCLK)/使能(OE) 实际没接对，移位寄存器不可能正常工作。
- 正确：`PC0=SER`、`PC1=SRCLK`、`PC2=RCLK`、`PC3=OE`、`PC4=PC4_ADC`。

**P1-2　疑似 VDD 接地（需肉眼核实）**
- 反查显示 U1 的 `VDD`(9脚) 连到 `GND`，且去耦 `C3` 两端都标 `GND`（正常应是 VDD─C3─GND）。
- 对比 `C8` 是正常的 `GND─+5V`，说明不是全局误标，而是 C3/VDD 局部接错。
- 后果若属实：MCU 电源脚被接地，通电即可能损坏芯片。**请先在画布上肉眼确认 U1.9 与 C3 的实际连线**，再决定修正为 `+5V`。

**LED-1　巨型无名飞线把全部输出短接**
- LED 页 67 条导线中，一根巨型多段导线 `net="(none)"` 把 595 的 QA~QH、所有 Rn、所有 Dn 的引脚端点连成**同一个无名网络**。
- 后果：逻辑上 40 路输出全部短接在一起，整板功能错误。这是当前**最大隐患**，必须用"每路独立网络 + 网络标签"替代。

**LED-2　595 未连 MCU、未级联、未供电**
- LED 页 595 的 `SRCLK/RCLK/OE` 引脚 = `UNWIRED`；级联 `QH'→下一片 SER` 未连；`VCC/GND` 未明确接到 `+5V/GND` 总线。
- 后果：595 既没被 MCU 驱动，也没确定供电。

**X-1　跨页网络名不一致 → SER 不导通**
- P1 页 SER 叫 `595 DATA（SER）`，LED 页叫 `PC0_SER`。EasyEDA 中同名网络标签才跨页导通，二者不同名 → **SER 信号在 MCU 与 595 之间实际断开**。
- 同理，SRCLK/RCLK/OE 在 LED 页根本没出现。

### 🟡 一般（文档/编号一致性）

**BOM-1　设计文档位号编号冲突**
- 文档 §16.1/§16.5 把 `R32`(电键保护)、`R33`(Q1基极)、`R34/R35`(CC下拉) 用于输入/电源电路；但 §16.4 又把 `R32~R37` 用作 LED 限流电阻（R32→D32、R33→D33…）。
- 原理图里 `R32~R36` 在两页重复出现（LED 页作限流、P1 页作输入/电源），应重编号避免同一工程内位号重复与混淆。

**BOM-2　部分位号未落到设计文档编号**
- P1 页的 `R38`(KEY 上拉)、`R32`(电键保护)、`C1/C2`(USB 入口滤波) 与设计文档一致，但 LED 页的 `R32~R37` 与 P1 页同名冲突（见 BOM-1）。

---

## 3. 修正方案

### 3.1 网络命名规范（统一，便于跨页网络标签）

| 信号 | 统一网络名 | 说明 |
|---|---|---|
| 595 串行数据 | `SER` | P1/U1.PC0 ↔ LED/U2.14（链首） |
| 595 移位时钟 | `SRCLK` | U1.PC1，五片 11 脚并联 |
| 595 锁存 | `RCLK` | U1.PC2，五片 12 脚并联 |
| 595 输出使能 | `OE` | U1.PC3，五片 13 脚并联（低有效，可 PWM 调光） |
| 电源 | `+5V` | USB VBUS 经滤波后；MCU VDD、595 VCC、LED 阳极链 |
| 地 | `GND` | 全板地 |
| 40 路输出 | `LED0`~`LED39` | 对应固件 `led_state` 的 bit0~bit39（见 §3.3 映射） |
| 级联（片间） | 直接导线 | U2.9→U3.14、U3.9→U4.14、U4.9→U5.14、U5.9→U6.14（同页直接连线即可） |
| 扩展输出 | `SER_OUT` | U6.9(QH') → J4-3 |

> 跨页连接策略：**采用同名网络标签**（P1 页与 LED 页用完全相同的网络名）。EasyEDA 中同名网络标签在整个 Schematic 内全局连通，正好满足"尽量使用网络标签"的要求，无需依赖 netport。

### 3.2 P1 页修正（低风险，先做）

1. 把 U1 `PC1` 的网络标签由 `PC4_ADC` 改为 `SRCLK`；`PC2`→`RCLK`；`PC3`→`OE`；`PC0` 的 `595 DATA（SER）`→`SER`。
2. 核实并修正 **P1-2**：确认 U1.9(VDD) 与 C3 的实际连线，将其接到 `+5V`（VDD─C3─GND，C3 一端 +5V、一端 GND）。
3. 确保 `UART_TX/UART_RX`、`PA1_KEY`、`PA2_CLR`、`PC4_ADC`、`SWIO`、`PD0_TONE`、`SPARE_*` 等本地网络标签齐全。
4. `+5V`/`GND` 在 P1 页靠近 MCU 与扩展排针处各放网络标签，作为跨页出口。

### 3.3 LED 页重画（核心，需删巨型飞线）

按设计文档 §16.3/§16.4 重建：

- **控制输入**：在 U2.14 放 `SER` 标签；U2~U6 的 11 脚(并联)放 `SRCLK`；12 脚放 `RCLK`；13 脚放 `OE`；10 脚(SRCLR)接 `+5V`（网络标签或直连）。
- **输出→LED 映射**（与设计文档 §8.3 一致，bit i 对应 LED i）：

| 595 | QA(15) | QB(1) | QC(2) | QD(3) | QE(4) | QF(5) | QG(6) | QH(7) |
|---|---|---|---|---|---|---|---|---|
| U2 | LED0/D1 | LED1/D2 | LED2/D3 | LED3/D4 | LED4/D5 | LED5/D6 | LED6/D7 | LED7/D8 |
| U3 | LED8/D9 | LED9/D10 | LED10/D11 | LED11/D12 | LED12/D13 | LED13/D14 | LED14/D15 | LED15/D16 |
| U4 | LED16/D17 | LED17/D18 | LED18/D19 | LED19/D20 | LED20/D21 | LED21/D22 | LED22/D23 | LED23/D24 |
| U5 | LED24/D25 | LED25/D26 | LED26/D27 | LED27/D28 | LED28/D29 | LED29/D30 | LED30/D31 | LED31/D32 |
| U6 | LED32/D33 | LED33/D34 | LED34/D35 | LED35/D36 | LED36/D37 | （空） | （空） | （空） |

- 每路：`595 Qx ── Rn(1kΩ) ── Dn 阳极；Dn 阴极 ── GND`。用网络标签 `LED0`~`LED39` 标在每段导线上（替代飞线）。
- **级联**：U2.9(QH')→U3.14(SER)、U3.9→U4.14、U4.9→U5.14、U5.9→U6.14，直接导线相连；U6.9→J4-3 标 `SER_OUT`。
- **供电**：每片 595 的 16(VCC)→`+5V`，8(GND)→`GND`，就近放 C4~C8 去耦。
- **删除**那根 `net="(none)"` 的巨型飞线，释放被短接的网络。

### 3.4 BOM 位号重编号（消解冲突）

将 P1 页输入/电源电路的电阻从与 LED 页冲突的 `R32~R36` 改为独立段，例如：
- 电键保护 `R32`(P1) → 保留 `R32`（仅 P1 用），LED 页限流电阻 `R32~R37` 改为 `RL1~RL37`（或 `R101~R137`），彻底避免两页同号。
- `R38`(KEY 上拉)、`R33`(Q1基极)、`R34/R35`(CC下拉) 维持 P1 页独占，LED 页不再使用这些号。

---

## 4. 建议执行顺序与验证

1. 先肉眼核实 **P1-2（VDD 接地）**，确认后再改。
2. P1 页：修正四脚网络标签（SER/SRCLK/RCLK/OE）+ 补 `+5V/GND` 跨页出口。
3. LED 页：删除巨型飞线 → 重建控制/级联/供电/40 路输出（全程网络标签）。
4. 重编号冲突位号。
5. 验证：生成网表（Netlist），确认 `SER/SRCLK/RCLK/OE/+5V/GND/LED0~LED39` 各自连通且互不短路；目检 DRC。

## 5. 待你确认/提供

- [ ] **P1-2（VDD 接地）是否为真？** 请画布肉眼确认 U1.9 与 C3 连线，决定修正方式。
- [ ] 跨页是否同意**统一用网络标签**（而非 netport）？
- [ ] LED 页 40 路输出网络名是否采用 `LED0~LED39`（与固件 bit 对齐）？
- [ ] 位号重编号方案是否接受（`RL1~RL37` 替代 LED 页 R32~R37）？

> 以上为审阅稿，未对工程做任何修改。确认后我可按第 4 节顺序执行。
