# 基于 CH32V003 的莫尔斯电码二叉树 LED 解析器
## 软件架构、程序规划与开发实施说明书

> **文档定位**：本文件只规定“程序应该怎么组织、各模块负责什么、模块之间怎么协作、开发顺序、调试方法、验收标准与已知风险”，**不提供最终业务代码**。  
> **硬件基线**：`Hardware Design Specification V1.4`（2026-08-12）。  
> **目标 MCU**：CH32V003F4P6（TSSOP-20）。  
> **推荐官方开发链**：MounRiver Studio / MounRiver Studio II + WCH-LinkE。  
> **推荐语言**：C。  
> **推荐运行模型**：裸机 + 1 ms 系统时基 + 非阻塞状态机。  
> **不建议**：Keil、RTOS、动态内存、长时间阻塞式 `Delay`、在中断中执行复杂业务逻辑。  

---

# 目录

- [1. 文档目标与开发原则](#1-文档目标与开发原则)
- [2. 从硬件 V1.4 继承的软件契约](#2-从硬件-v14-继承的软件契约)
- [3. 推荐开发环境与烧录方案](#3-推荐开发环境与烧录方案)
- [4. 软件总体架构](#4-软件总体架构)
- [5. 推荐工程目录](#5-推荐工程目录)
- [6. 模块清单与职责边界](#6-模块清单与职责边界)
- [7. 核心数据模型](#7-核心数据模型)
- [8. 状态机设计](#8-状态机设计)
- [9. 时间系统与调度策略](#9-时间系统与调度策略)
- [10. 莫尔斯解析逻辑的软件规划](#10-莫尔斯解析逻辑的软件规划)
- [11. LED 树显示逻辑的软件规划](#11-led-树显示逻辑的软件规划)
- [12. 输入、CLEAR、电位器、蜂鸣器与 UART 规划](#12-输入clear电位器蜂鸣器与-uart-规划)
- [13. 初始化顺序](#13-初始化顺序)
- [14. 开发阶段与里程碑](#14-开发阶段与里程碑)
- [15. 调试与 Bring-up 流程](#15-调试与-bring-up-流程)
- [16. 测试计划与验收标准](#16-测试计划与验收标准)
- [17. 高风险坑位清单](#17-高风险坑位清单)
- [18. 内存、Flash 与性能约束](#18-内存flash-与性能约束)
- [19. DEBUG / RELEASE 构建策略](#19-debug--release-构建策略)
- [20. 后续交给 Codex / AI 开发时的任务拆分](#20-后续交给-codex--ai-开发时的任务拆分)
- [21. 官方资料与推荐阅读顺序](#21-官方资料与推荐阅读顺序)
- [22. 最终交付物清单](#22-最终交付物清单)
- [23. 一页式开发路线图](#23-一页式开发路线图)

---

# 1. 文档目标与开发原则

## 1.1 最终软件要完成什么

本项目的软件最终需要实现：

1. CH32V003F4P6 上电初始化。
2. 读取板载 KEY 和外接电键共用的 `KEY` 输入。
3. 软件消抖。
4. 测量电键按下持续时间。
5. 根据当前基准时间 `T` 判断“点”与“划”。
6. 按莫尔斯二叉树规则推进 `node`。
7. 根据当前 `node` 点亮对应 LED。
8. 支持单点显示模式和路径显示模式。
9. 检测字符间隔并锁定当前字符。
10. 检测单词间隔并输出空格。
11. 通过 UART 输出解析结果和调试信息。
12. 读取电位器 ADC，调节点划速度。
13. CLEAR 短按清空，长按切换显示模式。
14. 蜂鸣器提供拍发侧音。
15. 对非法节点、非法码、越界等情况提供错误反馈。
16. 保留未来扩展标点、上位机命令、练习模式的接口。

---

## 1.2 软件设计原则

本项目应遵循下面几个原则。

### 原则 A：业务逻辑和硬件驱动分离

莫尔斯解析模块不能直接写 GPIO。

错误示例：

```text
morse_core.c
    └── 直接 GPIO_SetBits(PC0)
```

正确思路：

```text
morse_core
    ↓
生成“当前 node / 字符已锁定”等业务状态
    ↓
led_tree
    ↓
shift595
    ↓
GPIO
```

这样如果以后把 74HC595 换成别的 LED 驱动器，莫尔斯算法不需要修改。

---

### 原则 B：输入模块不理解莫尔斯码

`key_input` 模块只负责告诉上层：

```text
KEY_DOWN
KEY_UP
按下持续了多少 ms
```

它不应该知道：

- 什么是点；
- 什么是划；
- 什么是 E；
- 什么是 T；
- 什么是二叉树 node。

---

### 原则 C：尽量全部非阻塞

禁止业务模块依赖这种结构：

```text
亮灯
等待 500ms
灭灯
等待 500ms
```

原因是等待期间 MCU 无法可靠处理快速拍发。

推荐模型：

```text
记录“下一次状态切换时刻”
主循环不断轮询
时间到了再切换状态
```

---

### 原则 D：中断越简单越好

如果使用 SysTick/定时器中断作为 1 ms 系统时基：

中断里推荐只做：

```text
system_tick++
```

最多再置一个简单标志。

不要在中断中：

- `printf`
- 刷 40bit 595
- 做莫尔斯查表
- 做长按判断
- 闪灯
- 调用复杂业务代码

---

### 原则 E：所有魔法数字集中管理

例如：

```text
15 ms
60 ms
300 ms
2T
2.5T
6T
1.5 s
115200
```

不要散落在各个 `.c` 文件里。

全部放进配置层。

---

# 2. 从硬件 V1.4 继承的软件契约

本章内容来自本项目硬件 V1.4 设计，应视为**软件与 PCB 之间的硬性契约**。

---

## 2.1 MCU 与资源

MCU：

```text
CH32V003F4P6
封装：TSSOP-20
Flash：16 KB
RAM：2 KB
供电：本设计使用 +5V
```

由于 RAM 只有 2 KB，本项目不适合：

- RTOS；
- 大型协议栈；
- 动态内存；
- 大型字符串缓存；
- 重型 `printf`；
- 浮点格式化。

---

## 2.2 引脚分配

| 功能 | MCU 引脚 | 软件名称建议 | 备注 |
|---|---:|---|---|
| KEY | PA1 | `PIN_KEY` | 低有效 |
| CLEAR | PA2 | `PIN_CLEAR` | 低有效 |
| 速度 ADC | PC4 | `PIN_SPEED_ADC` | ADC 通道需按官方 EVT 核对 |
| 595 SER | PC0 | `PIN_595_SER` | 数据 |
| 595 SRCLK | PC1 | `PIN_595_SRCLK` | 移位时钟 |
| 595 RCLK | PC2 | `PIN_595_RCLK` | 锁存 |
| 595 OE | PC3 | `PIN_595_OE` | **低有效** |
| 蜂鸣器 | PD0 | `PIN_BUZZER` | 通过 S8050 驱动 |
| SWIO | PD1 | — | **禁止作为普通 IO** |
| UART TX | PD5 | `PIN_UART_TX` | 115200 |
| UART RX | PD6 | `PIN_UART_RX` | 预留 |
| NRST | PD7 | — | 复位 |
| 备用 | PC5/6/7、PD2/3/4 | — | 扩展 |

### 极重要

`PD1 = SWIO` 是烧录/调试生命线。

程序规划中：

```text
禁止初始化 PD1 为普通 GPIO
禁止把 PD1 加入“备用 IO 自动初始化”
禁止给 PD1 分配 LED、按键或 PWM
```

否则可能出现：

> 程序第一次烧进去成功，第二次 WCH-LinkE 连不上。

---

## 2.3 KEY 电气逻辑

KEY 节点：

```text
默认 = 高
按下 = 低
```

板载按键与外接电键最终都作用在同一个 KEY 网络。

软件统一处理：

```text
RAW_HIGH = released
RAW_LOW  = pressed
```

不要让板载 KEY 和外接电键形成两套逻辑。

---

## 2.4 CLEAR 电气逻辑

CLEAR：

```text
默认高
按下接地
```

需求：

```text
短按：回根 / 清空当前输入
长按 > 1s：切换显示模式
```

软件必须避免一次长按同时触发：

```text
“短按清空”
+
“长按切换”
```

---

## 2.5 莫尔斯时间契约

硬件 V1.4 给出的设计目标：

| 项目 | 判定 |
|---|---|
| 点 | 按下 `< 2T` |
| 划 | 按下 `>= 2T` |
| 字符结束 | 松开 `>= 2.5T` |
| 单词结束 | 松开 `>= 6T` |
| 消抖 | 15 ms |
| T 范围目标 | 60~300 ms |

这些值应该集中放在配置层，而不是分散到逻辑模块。

---

## 2.6 莫尔斯树节点契约

```text
START = node 1

输入点：
node = node * 2

输入划：
node = node * 2 + 1
```

例如：

```text
START = 1
  .
E = 2
  -
A = 5
  .
R = 10
```

---

## 2.7 LED 契约

5 片 74HC595：

```text
共 40 bit
实际使用 37 bit
```

软件逻辑：

```text
bit 0  -> D1  START
bit 1  -> D2  E
...
bit 36 -> D37 0
bit 37~39 -> NC
```

发送规则：

```text
bit39 最先发送
...
bit0 最后发送
```

最终要求：

```text
bit0 落在 #1 QA
```

这部分必须通过流水灯固件实机确认，不能只靠“看起来应该对”。

---

# 3. 推荐开发环境与烧录方案

---

## 3.1 推荐基线

建议本项目正式使用：

```text
IDE / 工程：
MounRiver Studio 或 MounRiver Studio II

编译器：
IDE 配套 RISC-V GCC 工具链

官方芯片例程：
CH32V003 EVT

下载 / 调试器：
WCH-LinkE

调试接口：
SWIO + GND + NRST

独立烧录工具：
WCH-LinkUtility
```

这是比 Keil 更适合 CH32V003 的路线。

---

## 3.2 为什么不建议在本项目里折腾 Keil

本项目核心目标是把板子稳定跑起来，而不是研究不同 IDE 的适配。

对 CH32V003：

- 沁恒有自己的 RISC-V 工具链；
- 有官方 EVT；
- 有官方 WCH-Link；
- 有官方烧录工具；
- 有针对 CH32 系列的工程模板。

因此没有必要把额外时间花在 Keil 授权、设备包、RISC-V 支持、下载算法等问题上。

---

## 3.3 “正版软件”烧录路线

### 路线 A：开发阶段

```text
MRS/MRS2
  ↓
Build
  ↓
生成 ELF / HEX / BIN
  ↓
WCH-LinkE
  ↓
SWIO
  ↓
CH32V003
```

### 路线 B：只烧录，不开发

```text
已经生成的固件
  ↓
WCH-LinkUtility
  ↓
WCH-LinkE
  ↓
PCB
```

这样以后就算只是帮朋友刷板，也不需要打开完整 IDE。

---

## 3.4 WCH-LinkE 与 PCB 连接建议

PCB H1 定义：

```text
H1-1 +5V
H1-2 GND
H1-3 SWIO
H1-4 NRST
```

调试连接至少需要：

```text
WCH-LinkE GND  -> PCB GND
WCH-LinkE SWIO -> PCB SWIO
WCH-LinkE RST  -> PCB NRST（建议连接）
```

供电必须提前决定：

### 模式 1：WCH-LinkE 给目标板供电

```text
Link VCC -> PCB +5V
USB-C 不再同时供电
```

### 模式 2：PCB USB-C 自己供电

```text
USB-C -> PCB +5V
WCH-LinkE 不向目标板输出电源
Link 与 PCB 共地
```

**不要默认让两个 5V 电源硬并联。**

---

# 4. 软件总体架构

推荐拆成 6 层。

```text
┌─────────────────────────────────┐
│ Layer 6：Application            │
│ app_controller / main           │
├─────────────────────────────────┤
│ Layer 5：Business Core          │
│ morse_core                      │
├─────────────────────────────────┤
│ Layer 4：Services               │
│ led_tree / diagnostics          │
│ input_manager / error_manager   │
├─────────────────────────────────┤
│ Layer 3：Drivers                │
│ shift595 / key / adc / uart     │
│ buzzer / clear                  │
├─────────────────────────────────┤
│ Layer 2：BSP                    │
│ gpio / timer / clock / pin cfg  │
├─────────────────────────────────┤
│ Layer 1：Vendor / WCH           │
│ startup / system / peripheral   │
│ CH32V003 EVT                    │
└─────────────────────────────────┘
```

---

## 4.1 Layer 1：WCH 官方层

尽量不改。

包含：

```text
启动文件
芯片头文件
系统时钟
中断定义
官方外设库
链接脚本
```

原则：

> 官方层尽量原样保留，项目自己的东西不要塞进去。

---

## 4.2 Layer 2：BSP

BSP 负责：

> “这块 PCB 上哪个功能接到了哪个 MCU 引脚。”

业务层不应该出现：

```text
GPIOC Pin 0
GPIOD Pin 5
```

而应该出现：

```text
595_SER
UART_TX
KEY
```

---

## 4.3 Layer 3：驱动层

只负责硬件操作。

例如：

```text
shift595
adc_speed
key_input
clear_input
buzzer
uart
```

---

## 4.4 Layer 4：服务层

把多个低层动作包装成业务可用服务。

例如：

```text
led_tree
input_manager
diagnostics
error_manager
```

---

## 4.5 Layer 5：莫尔斯核心

这是整个项目最应该保持“纯净”的模块。

输入：

```text
DOT
DASH
CHAR_TIMEOUT
WORD_TIMEOUT
RESET
```

输出：

```text
current_node
locked_char
invalid_code
```

它不应该知道：

- PA1；
- PC0；
- 74HC595；
- UART 寄存器；
- 蜂鸣器三极管。

---

## 4.6 Layer 6：Application

主要负责调度。

它回答：

```text
现在应该让谁运行？
某个事件应该交给哪个模块？
字符锁定后哪些服务要响应？
```

---

# 5. 推荐工程目录

建议工程结构：

```text
Morse_CH32V003/
│
├─ README.md
├─ docs/
│  ├─ software_architecture.md
│  ├─ hardware_notes.md
│  ├─ bringup_checklist.md
│  └─ known_issues.md
│
├─ User/
│  ├─ main.c
│  │
│  ├─ app/
│  │  ├─ app_controller.c
│  │  └─ app_controller.h
│  │
│  ├─ core/
│  │  ├─ morse_core.c
│  │  ├─ morse_core.h
│  │  ├─ morse_table.c
│  │  └─ morse_table.h
│  │
│  ├─ service/
│  │  ├─ led_tree.c
│  │  ├─ led_tree.h
│  │  ├─ input_manager.c
│  │  ├─ input_manager.h
│  │  ├─ diagnostics.c
│  │  ├─ diagnostics.h
│  │  ├─ error_manager.c
│  │  └─ error_manager.h
│  │
│  ├─ driver/
│  │  ├─ key_input.c
│  │  ├─ key_input.h
│  │  ├─ clear_input.c
│  │  ├─ clear_input.h
│  │  ├─ shift595.c
│  │  ├─ shift595.h
│  │  ├─ adc_speed.c
│  │  ├─ adc_speed.h
│  │  ├─ buzzer.c
│  │  ├─ buzzer.h
│  │  ├─ uart_debug.c
│  │  └─ uart_debug.h
│  │
│  ├─ bsp/
│  │  ├─ board.c
│  │  ├─ board.h
│  │  ├─ system_time.c
│  │  └─ system_time.h
│  │
│  └─ config/
│     ├─ app_config.h
│     └─ board_config.h
│
├─ Vendor/
│  └─ WCH official files
│
└─ test_firmware/
   ├─ 01_link_test/
   ├─ 02_uart_test/
   ├─ 03_led_chaser/
   ├─ 04_key_test/
   └─ 05_adc_test/
```

---

# 6. 模块清单与职责边界

---

## 6.1 `board_config`

### 负责

定义：

```text
每个功能对应的 MCU 引脚
GPIO 端口
有效电平
固定硬件参数
```

### 不负责

- 初始化业务；
- 莫尔斯解析；
- LED 动画；
- 按键状态机。

### 重要原则

其他模块尽量不要硬编码：

```text
PC0
PC1
PA1
```

---

## 6.2 `board`

### 负责

板级初始化入口。

例如概念上负责：

```text
clock
GPIO
UART
ADC
595
buzzer
system tick
```

### 不负责

进入莫尔斯业务逻辑。

---

## 6.3 `system_time`

### 负责

提供统一系统时间。

至少具备概念接口：

```text
now_ms()
elapsed_ms(start)
```

### 目标

整个项目只保留**一个时间基准**。

避免：

```text
key.c 自己一个计时器
clear.c 自己另一个 delay
morse.c 又一个循环计数
```

---

## 6.4 `key_input`

### 负责

- 读取 KEY 原始电平；
- 软件消抖；
- 检测稳定按下沿；
- 检测稳定松开沿；
- 测量按下时长；
- 生成输入事件。

### 输出事件建议

```text
KEY_EVENT_NONE
KEY_EVENT_DOWN
KEY_EVENT_UP
```

`UP` 事件同时携带：

```text
press_duration_ms
```

### 不负责

判断点划。

因为：

> 按下 120 ms 到底是点还是划，取决于 T，不属于按键驱动层。

---

## 6.5 `clear_input`

### 负责

CLEAR：

```text
消抖
按下
松开
短按
长按
```

### 输出事件建议

```text
CLEAR_NONE
CLEAR_SHORT
CLEAR_LONG
```

### 必须解决的问题

长按只能产生一次 LONG。

不能产生：

```text
LONG
LONG
LONG
LONG
```

也不能在松手后再补一个 SHORT。

---

## 6.6 `adc_speed`

### 负责

- 初始化 ADC；
- 定期采样 PC4；
- 滤波；
- 映射为速度参数 T；
- 提供当前建议 T。

### 推荐更新周期

```text
50~100 ms
```

没有必要每 1 ms 采样。

### 推荐滤波

简单即可：

```text
4 次或 8 次平均
```

不需要复杂数字滤波。

---

## 6.7 `shift595`

### 负责

74HC595 最底层输出。

输入：

```text
40bit 显示状态
```

输出：

```text
SER / SRCLK / RCLK 时序
```

### 不负责

- 字符；
- node；
- 路径；
- 闪烁语义。

### 最重要的软件契约

```text
bit39 first
bit0 last
```

---

## 6.8 `led_tree`

### 负责

把业务状态翻译成 LED 状态。

输入：

```text
node
显示模式
错误状态
锁定状态
```

输出：

```text
40bit led_state
```

然后调用：

```text
shift595
```

### 负责的功能

```text
显示 START
显示当前节点
显示从 START 到当前节点的路径
锁定字符显示
错误闪烁
全灯自检
```

### 不负责

判断点和划。

---

## 6.9 `buzzer`

### 负责

蜂鸣器：

```text
开始发声
停止发声
可选频率
```

### 推荐行为

KEY 有效按下：

```text
tone on
```

有效松开：

```text
tone off
```

### 注意

蜂鸣器动作不应该使用阻塞 Delay。

---

## 6.10 `uart_debug`

### 负责

- 初始化 UART；
- 输出字符；
- 输出调试日志；
- 可选接收未来命令。

### 建议分两种接口

业务输出：

```text
uart_output_char()
```

调试输出：

```text
debug_log()
```

这样 Release 可以关闭 debug，而保留最终字符输出。

---

## 6.11 `morse_table`

### 负责

保存：

```text
MORSE_CHAR[64]
NODE_TO_LED[64]
```

以及未来可能增加：

```text
NODE_VALID
NODE_DEPTH
```

### 原则

映射表是“数据”，不要散落在算法里。

---

## 6.12 `morse_core`

### 负责

纯莫尔斯逻辑。

核心状态：

```text
current_node
当前字符是否已开始
最后一次元素结束时间
当前字符使用的 T
是否已经锁定
```

### 输入

```text
dot
dash
time
reset
```

### 输出

```text
node changed
character locked
invalid
word gap
```

### 不负责

任何 GPIO。

---

## 6.13 `input_manager`

可选模块。

如果觉得项目规模很小，也可以不单独建立。

作用是统一接收：

```text
KEY
CLEAR
ADC
```

然后对应用层生成更高层事件。

---

## 6.14 `diagnostics`

### 负责

上电自检和 Bring-up。

例如：

```text
LED 流水
全灯闪烁
UART banner
ADC 值打印
按键状态打印
```

正式版本可关闭大部分诊断。

---

## 6.15 `error_manager`

### 负责

统一定义错误类型：

```text
MORSE_NODE_OVERFLOW
MORSE_INVALID_CODE
ADC_ABNORMAL
```

以及错误反馈方式：

```text
LED flash
UART log
reset current character
```

---

## 6.16 `app_controller`

### 负责

把整个系统串起来。

它是：

> “调度者”

而不是：

> “所有代码都塞进去的大 main.c”。

---

# 7. 核心数据模型

推荐至少存在这些核心概念。

---

## 7.1 `current_node`

范围：

```text
1~63
```

初始：

```text
1
```

---

## 7.2 `led_state`

逻辑上为 40bit。

硬件文档建议：

```text
uint64_t
```

优点：

```text
操作方便
bit mask 清晰
```

注意：

CH32V003 是小 MCU，64 位操作可能生成更多指令。

如果未来 Flash 非常紧，可以改为：

```text
uint8_t led_bytes[5]
```

但第一版没有必要为了省几条指令提前复杂化。

---

## 7.3 显示模式

建议枚举：

```text
LED_MODE_SINGLE
LED_MODE_PATH
```

不要用：

```text
0
1
```

直接散落在代码中。

---

## 7.4 当前字符状态

建议至少记录：

```text
current_node
char_started
last_release_ms
char_T_ms
char_locked
word_space_emitted
```

---

# 8. 状态机设计

---

## 8.1 KEY 消抖状态机

建议概念状态：

```text
RELEASED
PRESS_DEBOUNCE
PRESSED
RELEASE_DEBOUNCE
```

流程：

```text
RELEASED
  │ raw low
  ▼
PRESS_DEBOUNCE
  │ 连续 low >= 15ms
  ▼
PRESSED
  │ raw high
  ▼
RELEASE_DEBOUNCE
  │ 连续 high >= 15ms
  ▼
RELEASED
```

只有：

```text
PRESS_DEBOUNCE -> PRESSED
```

才产生真正 `KEY_DOWN`。

只有：

```text
RELEASE_DEBOUNCE -> RELEASED
```

才产生真正 `KEY_UP`。

---

## 8.2 CLEAR 状态机

建议：

```text
IDLE
DOWN
LONG_SENT
```

逻辑：

```text
按下
  ↓
DOWN
  ├─ 持续 < 1000ms 后松开 -> SHORT
  └─ 持续 >=1000ms       -> LONG -> LONG_SENT

LONG_SENT
  └─ 松开 -> IDLE
```

这样 LONG 不会重复触发。

---

## 8.3 莫尔斯字符状态

概念上可以分：

```text
IDLE
BUILDING
LOCKED_FEEDBACK
```

### IDLE

```text
node = 1
没有正在输入的字符
```

收到第一个点/划：

```text
进入 BUILDING
```

### BUILDING

每收到一个元素：

```text
更新 node
更新 LED
```

松开时间超过字符门限：

```text
锁定字符
输出
进入 LOCKED_FEEDBACK 或直接复位业务状态
```

### LOCKED_FEEDBACK

只负责视觉反馈，不阻塞下一个业务事件。

---

## 8.4 单词间隔状态

这是一个非常容易写出 Bug 的地方。

需求：

```text
2.5T -> 锁定字符
6T   -> 输出空格
```

问题：

如果 2.5T 时已经：

```text
node = 1
char_started = false
```

那么到了 6T，程序可能不知道刚才是否真的完成过一个字符。

因此必须单独记录：

```text
last_char_completed
word_space_emitted
```

单词空格：

```text
每个长间隔只能发一次
```

不能在主循环每 1 ms 输出一个空格。

---

# 9. 时间系统与调度策略

---

## 9.1 1 ms 系统时基

推荐：

```text
32bit 毫秒计数
```

例如概念：

```text
system_ms++
```

系统所有计时：

```text
消抖
按下时间
字符间隔
单词间隔
长按
LED 反馈
ADC 调度
```

全部来自该时基。

---

## 9.2 正确的时间差计算

不要使用：

```text
if(now > start + timeout)
```

推荐思想：

```text
now - start >= timeout
```

使用无符号整数时，可以自然处理计数器回绕问题。

---

## 9.3 不要在主业务里使用阻塞延时

特别禁止用于：

```text
错误闪烁
锁定闪烁
蜂鸣
CLEAR 提示
UART 后等待
```

---

## 9.4 T 的更新策略

电位器会随时变化。

一个很容易被忽略的问题：

> 用户按下 KEY 的过程中旋转了电位器。

如果程序实时读取 T：

```text
按下开始时 T=100ms
松开时 T=200ms
```

同一拍可能因为旋钮变化被重新解释。

### 推荐方案

ADC 模块持续产生：

```text
T_live
```

但莫尔斯字符开始后冻结：

```text
T_char
```

建议：

```text
第一个元素开始时：
T_char = T_live
```

直到该字符完成前，都使用同一个 `T_char`。

这样一个字符不会因为旋钮中途变化而改变判定标准。

---

# 10. 莫尔斯解析逻辑的软件规划

---

## 10.1 点划判断

在 KEY 松开事件时：

```text
press_duration < 2*T_char
    -> DOT
否则
    -> DASH
```

不要在按下过程中提前判断。

---

## 10.2 node 更新

DOT：

```text
node = node * 2
```

DASH：

```text
node = node * 2 + 1
```

更新后立即检查：

```text
node <= 63 ?
```

否则进入非法状态。

---

## 10.3 无定义节点

不是所有 node 都有对应字符。

例如扩展莫尔斯中的部分节点：

```text
Ü
Ä
未分配码
```

本项目没有安装对应 LED / 字符。

所以需要区分：

```text
node 合法存在
```

和：

```text
node 在本项目字符表中有效
```

最终锁定时如果：

```text
MORSE_CHAR[node] == '?'
```

按非法码处理。

---

## 10.4 字符锁定

满足：

```text
char_started == true
且
release_gap >= 2.5*T_char
```

则：

```text
读取 MORSE_CHAR[node]
```

如果有效：

```text
输出字符
视觉反馈
记录字符完成
```

如果无效：

```text
错误反馈
```

然后：

```text
业务 node 回根
```

---

## 10.5 单词间隔

满足：

```text
release_gap >= 6*T_char
```

且：

```text
刚刚确实结束过一个字符
```

且：

```text
space 尚未发送
```

才输出：

```text
' '
```

之后：

```text
space_emitted = true
```

直到下一次 KEY_DOWN 才允许下一个单词间隔重新产生空格。

---

# 11. LED 树显示逻辑的软件规划

---

## 11.1 node 和 LED 不是同一个编号

不要写：

```text
LED = node - 1
```

因为数字节点在 32~63 中不是连续的。

必须：

```text
node
 ↓
NODE_TO_LED[node]
 ↓
led index
```

---

## 11.2 单点模式

目标：

```text
只有当前 node 对应 LED 亮
```

通常建议 START 也作为当前节点处理。

---

## 11.3 路径模式

从当前节点一路回溯：

```text
node
node / 2
node / 4
...
1
```

把存在 LED 的节点全部点亮。

---

## 11.4 锁定反馈

推荐有两个实现方案。

### 方案 A

当前字符 LED 保持亮：

```text
约 1~1.5s
```

优点：

```text
简单
视觉清楚
```

### 方案 B

闪烁 3 次。

优点：

```text
反馈更明显
```

但一定要做成非阻塞动画。

---

## 11.5 错误反馈

建议：

```text
全树闪 3 次
```

但错误动画本身不能阻止：

```text
CLEAR
系统时间
必要输入扫描
```

如果错误动画期间不允许继续拍发，则应该明确把系统置于：

```text
ERROR_FEEDBACK
```

状态，而不是靠 Delay“假装不能输入”。

---

# 12. 输入、CLEAR、电位器、蜂鸣器与 UART 规划

---

## 12.1 KEY

推荐优先级：

```text
最高业务优先级
```

因为整个项目最重要的就是拍发时间准确。

---

## 12.2 ADC

推荐：

```text
每 50~100ms 采样
```

然后：

```text
ADC raw
 ↓
简单平均
 ↓
映射
 ↓
T_live
```

### 重要

硬件 V1.4 中曾使用：

```text
adc / 4095
```

作为映射表达式。

**程序里不要在没有核对 CH32V003 官方 ADC 文档和 EVT 的情况下直接把 `4095` 写死。**

正式开发时必须确认：

1. CH32V003 ADC 实际分辨率；
2. 官方库返回值范围；
3. 数据对齐方式；
4. 通道号；
5. ADC 时钟要求；
6. PC4 对应的实际 ADC channel 定义。

最好统一定义：

```text
ADC_FULL_SCALE
```

业务层永远只引用这个常量。

---

## 12.3 ADC 异常处理

如果 ADC 靠近：

```text
0
```

或者：

```text
满量程
```

这可能只是旋钮拨到端点，并不是故障。

因此不要简单把端点认为“断线”。

第一版只需限制：

```text
T_MIN <= T_live <= T_MAX
```

---

## 12.4 蜂鸣器

软件只需要：

```text
tone_on
tone_off
```

具体 PWM 初始化放驱动层。

如果 PWM 配置出现问题，第一阶段甚至可以：

```text
暂时不用蜂鸣器
```

先把核心莫尔斯功能跑通。

蜂鸣器属于“可后加”的模块，不应该卡住核心开发。

---

## 12.5 UART

建议：

```text
115200 8N1
```

最终业务：

```text
输出解码字符
```

调试期：

```text
KEY DOWN
KEY UP 87ms
T=120
DOT
node=2
LOCK E
```

---

## 12.6 UART 不能破坏实时输入

如果每 1 ms 都 `printf` 大量字符串，UART 可能阻塞 CPU。

因此：

### DEBUG

允许输出关键事件。

### RELEASE

只输出：

```text
最终字符
错误码（必要时）
```

不要持续打印 ADC。

---

# 13. 初始化顺序

推荐顺序：

```text
1. 系统时钟
2. 系统 1ms 时基
3. GPIO 基础状态
4. UART
5. 595 GPIO
6. 595 OE 保持安全状态
7. 清空 595 输出
8. KEY / CLEAR 输入
9. ADC
10. 蜂鸣器
11. 莫尔斯核心状态
12. LED 服务状态
13. 可选自检
14. 进入主循环
```

---

## 13.1 上电时避免 LED 乱闪

74HC595 初始化时：

```text
SER / CLK / LATCH
```

可能处在不确定状态。

推荐：

```text
先保证 OE 处于关闭输出状态
初始化移位寄存器为 0
锁存
再使能 OE
```

注意：

```text
OE 是低有效
```

不要写反。

---

# 14. 开发阶段与里程碑

最重要原则：

> 不要第一次就写“完整莫尔斯程序”。

---

## M0：工程能编译

### 目标

```text
官方 CH32V003 工程可以编译
```

### 验收

```text
0 error
生成 ELF/HEX/BIN
```

---

## M1：WCH-LinkE 通信

### 目标

```text
识别芯片
下载
复位
再次下载
```

### 验收

连续烧录至少 5 次正常。

重点确认：

```text
PD1 没有被程序占用
```

---

## M2：UART

### 目标

上电打印：

```text
Morse CH32V003 Boot
```

### 验收

电脑串口助手稳定接收。

---

## M3：595 + LED 流水灯

### 目标

```text
D1
D2
...
D37
```

依次点亮。

### 这个阶段验证

- SER；
- SRCLK；
- RCLK；
- OE；
- 595 级联；
- LED 极性；
- 40bit 位序；
- `NODE_TO_LED` 物理基础。

这是全项目最重要的硬件验收固件。

---

## M4：KEY

### 目标

UART 输出：

```text
KEY DOWN
KEY UP duration=xxx
```

### 验收

- 快速拍发不丢；
- 机械抖动不会产生多个事件；
- 板载 KEY 正常；
- 外接电键正常。

---

## M5：ADC

### 目标

旋转电位器：

```text
ADC
T
```

平滑变化。

### 验收

T 覆盖目标速度范围。

---

## M6：纯莫尔斯核心

暂时可以：

```text
不接 LED
```

只用 UART。

输入：

```text
.-.
```

应该得到：

```text
R
```

---

## M7：莫尔斯 + LED

现在才把：

```text
node
```

接给：

```text
led_tree
```

---

## M8：CLEAR + 模式

加入：

```text
短按 reset
长按 single/path
```

---

## M9：蜂鸣器 + 错误动画 + 自检

最后加“锦上添花”功能。

---

# 15. 调试与 Bring-up 流程

PCB 焊完以后建议严格按下面顺序。

---

## 15.1 不上电

检查：

```text
MCU 方向
595 方向
LED 极性
USB-C
电解电容
短路
焊桥
H1 针序
```

---

## 15.2 测电源

先确认：

```text
VBUS
+5V
GND
```

正常。

---

## 15.3 MCU 最小通信

WCH-LinkE：

```text
能识别
能擦除
能下载
能复位
```

否则先不要调莫尔斯。

---

## 15.4 LED

跑独立流水灯。

如果：

```text
D1 实际亮的是 D8
```

说明位序/接线与软件理解不一致。

先修映射。

不要靠改莫尔斯算法“补偿”。

---

## 15.5 KEY

用 UART 测实际时间。

用手：

```text
短按
长按
快速连按
```

检查是否存在：

```text
一次按键产生 2~5 次事件
```

---

## 15.6 ADC

记录几个位置：

```text
最左
1/4
中间
3/4
最右
```

看 ADC 单调性。

---

## 15.7 莫尔斯

测试：

```text
E .
T -
R .-.
S ...
O ---
SOS ... --- ...
```

---

## 15.8 数字

必须测试完整 5 元素：

```text
1 .----
5 .....
0 -----
```

---

# 16. 测试计划与验收标准

---

## 16.1 KEY 单元测试

| 测试 | 预期 |
|---|---|
| 5 ms 毛刺 | 不产生有效按下 |
| 10 ms 毛刺 | 不产生有效按下 |
| 稳定按下 >15ms | 产生一次 DOWN |
| 松开抖动 | 只产生一次 UP |
| 连续快速点按 | 每次均产生一对 DOWN/UP |

---

## 16.2 点划边界测试

重点测试：

```text
接近 2T
```

例如 T=100ms：

```text
190ms
199ms
200ms
201ms
210ms
```

确认边界定义与实现一致。

---

## 16.3 字符间隔边界

重点：

```text
2.4T
2.5T
2.6T
```

---

## 16.4 单词间隔边界

重点：

```text
5.9T
6.0T
6.1T
```

并确认：

> 一个长空白只输出一个空格。

---

## 16.5 全字符测试

A~Z：

```text
26 个
```

数字：

```text
0~9
```

共：

```text
36 个字符
```

逐个测试。

---

## 16.6 非法输入测试

例如走到：

```text
未定义 node
```

预期：

```text
不输出错误字符
产生明确错误反馈
恢复到可继续输入状态
```

---

## 16.7 长时间测试

连续使用：

```text
10~30 分钟
```

检查：

- 状态是否卡死；
- 空格是否越来越多；
- LED 是否错位；
- ADC 是否导致突变；
- UART 是否影响输入。

---

# 17. 高风险坑位清单

按照严重程度划分：

```text
P0：可能导致板子无法继续正常下载 / 核心完全错误
P1：主要功能错误
P2：体验或维护性问题
```

---

## P0-01：把 PD1/SWIO 当普通 GPIO

### 后果

下一次下载/调试困难甚至失败。

### 规避

```text
board_config 中明确 reserved
任何 GPIO 初始化列表不得包含 PD1
```

---

## P0-02：USB-C 与 WCH-LinkE 双路 5V 供电

### 后果

可能产生：

```text
反向灌电
电源打架
不可预测行为
```

### 规避

开发前明确目标板供电来源。

---

## P0-03：595 位序理解错误

### 现象

```text
点 E 亮了别的 LED
数字完全错位
```

### 规避

先做 D1~D37 独立流水测试。

---

## P0-04：OE 极性写反

74HC595：

```text
OE = 低有效
```

如果按“高有效”理解，会导致：

```text
初始化后灯全灭
或者显示逻辑反常
```

---

## P1-01：直接把 ADC 满量程写死为 4095

硬件文档里有 `/4095` 的规划表达式，但正式程序必须按 CH32V003 官方 ADC 文档和 EVT 确认实际返回范围。

### 规避

统一：

```text
ADC_FULL_SCALE
```

然后让映射函数依赖该常量。

---

## P1-02：字符完成后丢失单词间隔状态

典型错误：

```text
2.5T
-> node=1
-> char_started=false
```

到了 6T：

```text
程序不知道要输出空格
```

### 规避

单独保存：

```text
last_char_completed
space_emitted
```

---

## P1-03：每 1ms 输出一次空格

条件：

```text
release_gap >= 6T
```

会持续为真。

如果没有 latch：

```text
UART 会疯狂输出空格
```

---

## P1-04：CLEAR 长按同时触发短按

### 规避

LONG 一旦发出：

```text
release 时禁止 SHORT
```

---

## P1-05：T 在一个字符中途改变

用户转动电位器会造成当前字符判定规则变化。

### 规避

每个字符冻结：

```text
T_char
```

---

## P1-06：使用阻塞 Delay 做 LED 动画

### 后果

快速 KEY 输入可能直接漏掉。

---

## P1-07：大量 printf 阻塞

CH32V003 资源小。

如果每个主循环打印：

```text
ADC
node
time
GPIO
```

会严重干扰系统。

---

## P1-08：`printf("%f")`

浮点格式化可能显著增加代码体积。

本项目没必要。

例如 T：

```text
120 ms
```

完全可以用整数。

---

## P1-09：蜂鸣器 PWM 引脚/复用未按官方例程核对

硬件规划将 PD0 用于蜂鸣器。

正式写驱动时必须：

1. 根据 CH32V003 参考手册；
2. 根据 EVT 中 TIM/PWM 示例；
3. 核对对应定时器、通道、复用映射。

不要只凭“STM32 类似”直接搬代码。

---

## P1-10：照搬 STM32 HAL 代码

CH32 外设命名、库接口、启动文件、RISC-V 中断系统都可能与 STM32 不同。

可以借鉴思想：

```text
GPIO
ADC
TIM
USART
```

但不要直接复制 HAL API。

---

## P1-11：ISR 中做复杂工作

### 错误

```text
1ms ISR
 ├ printf
 ├ ADC
 ├ 595
 └ Morse
```

### 正确

```text
ISR
 └ tick++
```

---

## P1-12：变量在 ISR 和主循环间共享但未正确处理

例如：

```text
system_ms
```

如果中断修改、主循环读取，应根据实现方式正确考虑：

```text
volatile
原子性
```

---

## P2-01：把全部代码写在 main.c

短期好像快。

后面：

```text
1000 行 main.c
```

会非常难让人或 AI 修改。

---

## P2-02：模块循环依赖

禁止形成：

```text
morse -> led
led -> morse
```

推荐单向依赖。

---

## P2-03：直接暴露全局变量

不要让所有文件都：

```text
extern current_node
extern led_state
extern T
```

优先通过模块接口访问。

---

## P2-04：使用 malloc/free

没必要。

2 KB RAM 下还可能带来碎片。

---

## P2-05：加入 RTOS

这个项目本质上只有：

```text
几个按键
ADC
595
UART
时间状态机
```

裸机轮询已经足够。

RTOS 会增加：

```text
RAM
Flash
调试难度
上下文复杂度
```

---

# 18. 内存、Flash 与性能约束

---

## 18.1 RAM

目标 MCU RAM 很小。

建议：

```text
禁止大型局部数组
禁止递归
禁止动态内存
避免大字符串
```

莫尔斯表放：

```text
const
```

让编译器尽量放 Flash。

---

## 18.2 Flash

要特别注意：

```text
printf
浮点库
大型格式化
通用库
```

Debug 可以保留更多日志。

Release 应裁剪。

---

## 18.3 64bit

`uint64_t led_state`：

优点大于缺点。

只有当最终发现：

```text
Flash/性能确实吃紧
```

再考虑优化成 5 字节数组。

---

## 18.4 主循环频率

主循环不要求固定 1 kHz。

只需要：

```text
尽可能快速循环
```

然后所有时间事件根据 `now_ms()` 判断。

---

# 19. DEBUG / RELEASE 构建策略

推荐至少两个模式。

---

## DEBUG

开启：

```text
UART debug
节点打印
按下时间
ADC/T
错误原因
启动 banner
```

---

## RELEASE

关闭：

```text
持续 ADC log
详细 node log
大段文本
```

保留：

```text
实际解码字符
必要错误反馈
```

---

## BRINGUP

还可以增加第三种：

```text
BRINGUP
```

专门用于：

```text
硬件第一次上电
LED 流水
GPIO 测试
ADC 原始值
```

---

# 20. 后续交给 Codex / AI 开发时的任务拆分

不要给 AI 一个任务：

> “帮我写完整程序。”

建议按模块逐步交付。

---

## AI Task 01：建立官方工程骨架

只允许：

```text
基于 CH32V003 EVT
建立工程
能够编译
```

不要写业务。

---

## AI Task 02：system_time

目标：

```text
稳定 1ms 时基
```

验收：

```text
UART 每 1000ms 输出一次计数
```

---

## AI Task 03：UART

目标：

```text
115200 8N1
```

只写 UART。

---

## AI Task 04：595

目标：

```text
bit39 first
bit0 last
D1~D37 流水
```

明确告诉 AI：

> 不允许写莫尔斯逻辑。

---

## AI Task 05：KEY

目标：

```text
15ms 软件消抖
DOWN/UP event
duration
```

---

## AI Task 06：ADC

目标：

```text
按 EVT 正确初始化 PC4 ADC
输出 raw
映射 T
```

要求 AI：

> 先根据官方 CH32V003 EVT 的 ADC 例程确认 ADC 分辨率、通道和返回范围，不得假设 12bit。

---

## AI Task 07：morse_core

要求：

```text
纯 C
不引用 GPIO
不引用 WCH 外设库
```

这样甚至可以在 PC 上单元测试。

---

## AI Task 08：led_tree

输入：

```text
node
mode
```

输出：

```text
led_state
```

---

## AI Task 09：整合

最后才把：

```text
key
morse
LED
UART
ADC
```

接起来。

---

## AI Task 10：CLEAR / buzzer / animation

最后加入。

---

## 20.1 给 AI 修改代码时的规则

建议项目根目录增加：

```text
AGENTS.md
```

内容明确：

```text
1. 不修改 Vendor 目录。
2. PD1 永远保留 SWIO。
3. 不使用动态内存。
4. 不引入 RTOS。
5. 不在业务代码使用阻塞 Delay。
6. morse_core 禁止访问 GPIO。
7. NODE_TO_LED 是唯一 node->LED 映射。
8. 595 必须 bit39 first。
9. 任何 ADC 满量程假设必须引用官方资料。
10. 修改后必须保持 MRS/MRS2 工程可编译。
```

这个对 Codex 特别有用。

---

# 21. 官方资料与推荐阅读顺序

以下优先使用**沁恒/WCH 官方资料**。

---

## 21.1 CH32V003 产品页

<https://www.wch.cn/products/CH32V003.html>

用途：

```text
确认芯片系列
找数据手册
找参考手册
找开发资源
```

---

## 21.2 CH32V003 数据手册

<https://www.wch.cn/downloads/CH32V003DS0_PDF.html>

开发前重点看：

```text
封装引脚
供电
GPIO
电气特性
复用功能
```

---

## 21.3 CH32V00x 参考手册

<https://www.wch.cn/downloads/ch32v00xrm_pdf.html>

重点章节：

```text
RCC / Clock
GPIO
ADC
USART
TIM
SysTick/系统时基相关内容
FLASH
中断
```

---

## 21.4 CH32V003 EVT

<https://www.wch.cn/downloads/ch32v003evt_zip.html>

这是程序开发最重要的参考之一。

优先找类似：

```text
GPIO
ADC
USART
TIM/PWM
SysTick
Debug
```

的官方例程。

原则：

> 外设初始化优先参考 EVT，而不是先去网上抄 STM32 代码。

---

## 21.5 WCH RISC-V 产品与工具总览

<https://special.wch.cn/zh_cn/RISCV_MCU_Index/>

可以确认：

```text
沁恒 RISC-V MCU 生态
MounRiver Studio
MounRiver Studio II
官方调试工具体系
```

---

## 21.6 MounRiver Studio

官网：

<http://www.mounriver.com/>

下载入口通常为：

<http://www.mounriver.com/download>

建议：

```text
下载当前官方稳定版
不要在项目中途随意升级 IDE
```

一旦工程可以稳定编译，记录版本号。

---

## 21.7 WCH-Link

<https://www.wch.cn/products/WCH-Link.html>

用途：

```text
了解 WCH-Link / WCH-LinkE
下载/在线调试
接口和模式
```

---

## 21.8 WCH-LinkUtility

<https://www.wch.cn/downloads/WCH_LinkUtility_ZIP.html>

适合：

```text
已有 HEX/BIN 后单独烧录
不需要打开完整 IDE
```

---

## 21.9 WCH 下载中心

<https://www.wch.cn/downloads/>

如果上面的直链未来变化：

```text
直接在下载中心搜索
CH32V003
CH32V00x
WCH-Link
WCHISPTool
```

---

## 21.10 WCHISPTool

WCH 官方当前工具列表中包含对 `CH32V00x` 系列的 ISP 烧录支持。

但本 PCB 已经明确设计了：

```text
SWIO + WCH-LinkE
```

因此本项目第一选择仍然是：

```text
WCH-LinkE
```

ISP 作为备用路线即可。

---

# 22. 最终交付物清单

项目完成后建议仓库至少包含：

```text
README.md

docs/
├ hardware_reference.md
├ software_architecture.md
├ bringup.md
├ pinout.md
└ known_issues.md

firmware/
├ MRS/MRS2 project
├ source
└ build config

release/
├ firmware_v1.0.hex
├ firmware_v1.0.bin
└ CHANGELOG.md
```

---

## 22.1 README 应写清楚

```text
项目是什么
MCU 型号
PCB 版本
软件版本
开发 IDE 版本
WCH-Link 连接方式
如何编译
如何下载
UART 参数
已知问题
```

---

## 22.2 Release 固件命名

不要：

```text
test.hex
new.hex
new2.hex
final.hex
final_final.hex
```

推荐：

```text
morse_ch32v003_v0.1.0.hex
morse_ch32v003_v1.0.0.hex
```

---

# 23. 一页式开发路线图

如果只看这一章，可以按以下顺序执行：

```text
① 安装官方 MRS/MRS2
        ↓
② 下载 CH32V003 EVT
        ↓
③ 安装/连接 WCH-LinkE
        ↓
④ 从官方 CH32V003 示例工程建立项目
        ↓
⑤ 保留 PD1/SWIO，确认可反复下载
        ↓
⑥ UART Hello
        ↓
⑦ 74HC595 D1~D37 流水灯
        ↓
⑧ KEY 消抖 + 按下时间
        ↓
⑨ ADC 读取 + 确认满量程/通道
        ↓
⑩ 纯 morse_core
        ↓
⑪ NODE_TO_LED
        ↓
⑫ 单点模式
        ↓
⑬ 路径模式
        ↓
⑭ 字符 2.5T 锁定
        ↓
⑮ 单词 6T 空格（只发一次）
        ↓
⑯ CLEAR 短按
        ↓
⑰ CLEAR 长按切模式
        ↓
⑱ 蜂鸣器
        ↓
⑲ 非阻塞错误动画
        ↓
⑳ A-Z + 0-9 全表测试
        ↓
Release
```

---

# 附录 A：建议固定的软件参数

这些参数应集中在 `app_config.h`，这里只规定“应该集中管理”，具体值仍以调试结果为准。

| 参数 | V1.4 初始规划 |
|---|---:|
| KEY debounce | 15 ms |
| CLEAR debounce | 15 ms 左右 |
| CLEAR long press | 1000 ms |
| T min | 60 ms |
| T max | 300 ms |
| Dot/Dash threshold | 2T |
| Character gap | 2.5T |
| Word gap | 6T |
| UART | 115200 8N1 |
| ADC update | 50~100 ms |
| Lock feedback | ~1~1.5 s 或非阻塞闪烁 |

---

# 附录 B：推荐模块依赖关系

```text
main
 └ app_controller
      ├ morse_core
      │   └ morse_table
      │
      ├ key_input
      │   ├ board
      │   └ system_time
      │
      ├ clear_input
      │   ├ board
      │   └ system_time
      │
      ├ adc_speed
      │   └ board
      │
      ├ led_tree
      │   ├ morse_table
      │   └ shift595
      │       └ board
      │
      ├ buzzer
      │   └ board
      │
      └ uart_debug
          └ board
```

禁止反向依赖：

```text
shift595 -> led_tree       ×
key_input -> morse_core    ×
morse_core -> GPIO         ×
morse_table -> app         ×
```

---

# 附录 C：第一版软件的“必须有”和“可以后做”

## 必须有

```text
官方工程能编译
WCH-LinkE 能反复下载
1ms 时间基准
595
LED 映射
KEY 消抖
点划判断
node
字符锁定
UART
ADC T
CLEAR short
```

## 第二优先级

```text
路径模式
CLEAR long
蜂鸣器
错误动画
```

## V2 再考虑

```text
自适应速度
练习模式
UART RX 命令
标点符号
上位机
自动单元测试框架
```

---

# 附录 D：开发时最值得反复确认的十件事

1. `PD1` 是否始终保留给 SWIO。
2. WCH-LinkE 与 USB-C 是否出现双电源。
3. 595 的 OE 是否按低有效处理。
4. `bit39 first / bit0 last` 是否与实板一致。
5. D1~D37 流水灯是否完全按位号顺序。
6. KEY 15ms 消抖是否会吃掉正常短点。
7. ADC 返回范围是否由 CH32V003 官方资料确认，而不是假设 4095。
8. 一个字符输入过程中 T 是否被冻结。
9. 6T 单词间隔是否只输出一个空格。
10. 所有 LED 动画和蜂鸣反馈是否都没有阻塞 KEY 输入。

---

# 结论

本项目的软件复杂度并不高，真正需要控制的是**边界和时序**。

最合适的实现不是“写一个巨大 `main.c`”，而是：

```text
官方 WCH 底层
    ↓
干净的驱动层
    ↓
纯莫尔斯核心
    ↓
非阻塞状态机
    ↓
应用调度
```

只要开发顺序坚持：

```text
下载
→ UART
→ 595
→ KEY
→ ADC
→ Morse
→ LED
→ 集成
```

而不是一次性写完全部功能，这块板的调试风险会低很多。

**第一版最重要的三个独立测试固件：**

```text
1. LED D1~D37 流水灯
2. KEY 按下/松开时长 UART 输出
3. ADC raw/T UART 输出
```

这三个通过以后，再开始完整莫尔斯业务，是最稳妥的开发路线。
